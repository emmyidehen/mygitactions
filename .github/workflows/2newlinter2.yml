name: New-Linter2

on: push

jobs:
  new-lint:
    name: Lint code base
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
          env;
          DEFAULT_BRANCH:main
          GITHUB_TOKEN:${{ secrets.GITHUB_TOKEN }}

